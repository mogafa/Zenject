name: UPM CI

# Controls when the action will run. 
on:
  # Triggers the workflow on push or pull request events but only for the main branch
  push:
    branches: [ for_upm ]
  pull_request:
    branches: [ for_upm ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  split-upm:
    name: split upm branch
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: 14
          registry-url: https://registry.npmjs.org/
      # Runs a single command using the runners shell
      - name: Split core upm branch
        run: |
          git push origin --delete upm &> /dev/null || echo upm branch not found
          git subtree split -P "$CORE_PKG_ROOT" -b upm
          git push -u origin upm
          git checkout upm
          git push https://lltxjenkins:lltxGame123!@code.aliyun.com/upm/zenject.git
          npm publish . --access public
        env:
          CORE_PKG_ROOT: UnityProject/Assets/Plugins/Zenject
          NODE_AUTH_TOKEN: ${{secrets.NUGET_TOKEN}}
